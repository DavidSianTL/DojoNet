@model ProyectoDojoGeko.Models.Usuario.UsuarioFormViewModel
@{
    ViewData["Title"] = "Crear Nuevo Usuario";
    Layout = "~/Views/Shared/_DashboardLayoutSistema.cshtml";
}

<!-- CSS específico para el formulario de usuarios -->

<div class="empleado-form-container">
    <!-- Header de la pagina -->
    <div class="page-header">
        <div class="header-content">
            <h1 class="page-title">
                <i class="fas fa-user-plus"></i>
                 Crear Usuario
            </h1>
            <p class="page-subtitle">
                Ingresa los datos del nuevo usuario
            </p>
        </div>
        <div class="header-actions">
            <a href="@Url.Action("Index", "Usuarios")" class="btn-back">
                <i class="fas fa-arrow-left"></i>
                <span>Volver a la Lista</span>
            </a>
        </div>
    </div>

    <!-- Formulario principal -->
    <div class="form-card">
        <form asp-action="Crear" method="post">
          

            <!-- Informacion Personal -->
            <div class="form-section">

                <div class="form-grid">
                    @Html.AntiForgeryToken()

                    <!-- Username -->
                    <div class="form-group">
                        <label asp-for="Usuario.Username" class="form-label">
                            <div class="label-icon">
                                <i class="fas fa-user"></i>
                            </div>
                            <span>Usuario</span>
                            <span class="required">*</span>
                        </label>
                        <div class="input-container">
                            <input asp-for="Usuario.Username" class="form-control" placeholder="Ingrese el usuario"/>
                            <span asp-validation-for="Usuario.Username" class="MensajeError"></span>
                        </div>
                    </div>

                    <!-- Empleado -->
                    <div class="form-group">
                        <label asp-for="Usuario.FK_IdEmpleado" class="form-label">
                            <div class="label-icon">
                                <i class="fas fa-user-tie"></i>
                            </div>
                            <span>Empleado</span>
                            <span class="required">*</span>
                        </label>
                        <select asp-for="Usuario.FK_IdEmpleado" asp-items="Model.Empleados" class="form-control">
                            <option value="">Selecciona un empleado</option>
                        </select>
                        <span asp-validation-for="Usuario.FK_IdEmpleado" class="MensajeError"></span>
                    </div>
                    
                </div>
            </div>           

            <!-- Botones de accion -->
            <div class="form-actions">
                <div class="actions-container">
                    <button type="button" class="btn-cancel" onclick="window.location.href='@Url.Action("Index", "Usuarios")'">
                        <i class="fas fa-times"></i>
                        <span>Cancelar</span>
                    </button>

                    <button type="reset" class="btn-reset" id="resetBtn">
                        <i class="fas fa-undo"></i>
                        <span>Limpiar</span>
                    </button>

                    <button type="submit" class="btn-submit" id="submitBtn">
                        <i class="fas fa-plus"></i>
                        <span>Crear Usuario</span>
                    </button>
                </div>
            </div>

        </form>
    </div>
</div>

<!-- Modal de confirmacion -->
<div class="modal-overlay" id="confirmModal">
    <div class="modal-container">
        <div class="modal-header">
            <h3>Confirmar Accion</h3>
            <button class="modal-close" id="closeModal">
                <i class="fas fa-times"></i>
            </button>
        </div>
        <div class="modal-body">
            <div class="modal-icon success">
                <i class="fas fa-question-circle"></i>
            </div>
            <p id="modalMessage">Esta seguro que desea guardar los cambios?</p>
        </div>
        <div class="modal-footer">
            <button class="btn-cancel" id="cancelAction">
                <i class="fas fa-times"></i>
                Cancelar
            </button>
            <button class="btn-confirm" id="confirmAction">
                <i class="fas fa-check"></i>
                Confirmar
            </button>
        </div>
    </div>
</div>

@section Styles {
    <link rel="stylesheet" href="~/css/empleado-form.css" />
}

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script src="~/js/empleado-form.js"></script>
}
