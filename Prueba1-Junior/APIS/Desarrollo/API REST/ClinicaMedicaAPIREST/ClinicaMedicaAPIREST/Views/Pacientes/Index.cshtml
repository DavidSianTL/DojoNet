@model List<Paciente>

    
@{
    ViewData["Title"] = "Pacientes";
    
}

<h1>Pacientes</h1>

@if (Model.Count < 0)
{
    <p>No hay pacientes registrados.</p>
}
else
{
    @foreach (var paciente in Model)
    {
        <p>@paciente</p>
    }
}